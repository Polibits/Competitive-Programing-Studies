#include <bits/stdc++.h>

using namespace std;

int main() {
    // declaração das variáveis
    float a, b, x, y;

    // leitura das variáveis
    cin >> a >> b >> y;
  
    // cálculo de x
    x = y*(a/b);

    // exibição de x
    cout << x << "\n";
  
    return 0;
}